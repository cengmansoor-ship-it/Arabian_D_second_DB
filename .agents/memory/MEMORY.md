- [pnpm workspace: sqlite3 native build](sqlite3-native-build.md) — sqlite3 needs prebuild-install (works out of the box); node-gyp fallback needs python3 but fails on 3.12 (no distutils) — don't try to fix node-gyp, prebuild-install already works.
- [Artifact without registered workflow](artifact-missing-workflow.md) — some pre-existing `artifacts/*/.replit-artifact/artifact.toml` dirs aren't in `listArtifacts()`/no managed workflow; use plain `configureWorkflow` with an inlined `PORT=...` in the command (no `env` param exists).
- [Arabian D frontend/backend wiring](arabian-d-frontend-backend-wiring.md) — Vite proxy + CORS credentials setup between `@workspace/web` and `@workspace/api-server`, and the requirePermission/recordAudit RBAC pattern to reuse for every new module.
- [Sequelize + SQLite nested transaction pitfalls](sequelize-sqlite-transactions.md) — no CLS configured; shared posting helpers must accept an optional existing transaction, and every balance-recompute read must thread the active transaction or it can see stale pre-write data.
- [Multi-currency journal posting pattern](multi-currency-posting.md) — use a clearing account so each currency's lines balance independently when a ledger enforces per-currency debit=credit.
